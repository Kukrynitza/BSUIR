@startuml
title Инфраструктура - Система анализа кредитоспособности

/' Общие настройки '/
skinparam classAttributeIconSize 0
hide empty members

/' ======================= '/
/' ПАКЕТ: ИНФРАСТРУКТУРА '/
/' ======================= '/
package "Инфраструктура" {

    class КонтейнерЗависимостей <<injector>> {
        + зарегистрировать(интерфейс: Class, реализация: Class): void
        + разрешить(интерфейс: Class): Object
        + создатьКонтроллер(тип: Class): Object
        + создатьПредставление(тип: Class): Object
    }

    class ФабрикаКонтроллеров <<injector>> {
        + создатьОтчетныйКонтроллер(репозитории: List): ОтчетныйКонтроллер
        + создатьСистемныйКонтроллер(репозитории: List): СистемныйКонтроллер
        + создатьКредитныйКонтроллер(репозитории: List): КредитныйКонтроллер
        + создатьИнтеграционныйКонтроллер(репозитории: List): ИнтеграционныйКонтроллер
    }

    class ФабрикаПредставлений <<injector>> {
        + создатьПредставлениеОтчетов(контроллер1: ОтчетныйКонтроллер, контроллер2: СистемныйКонтроллер): ПредставлениеОтчетов
        + создатьПредставлениеАнализа(контроллер: КредитныйКонтроллер): ПредставлениеАнализа
    }

    class БазовыйРепозитории

    class Представление
    class ОтчетныйКонтроллер
    class СистемныйКонтроллер
    class КредитныйКонтроллер
    class ИнтеграционныйКонтроллер

    /' ----------------------- '/
    /' СВЯЗИ '/
    /' ----------------------- '/
    КонтейнерЗависимостей "1" o-- "1" ФабрикаПредставлений : о фабрикаПредставлений
    КонтейнерЗависимостей "1" o-- "1" ФабрикаКонтроллеров : о фабрикаКонтроллеров

    ФабрикаПредставлений ..> Представление : <<создает>>

    ФабрикаКонтроллеров ..> ОтчетныйКонтроллер : <<создает>>
    ФабрикаКонтроллеров ..> КредитныйКонтроллер : <<создает>>
    ФабрикаКонтроллеров ..> ИнтеграционныйКонтроллер : <<создает>>
    ФабрикаКонтроллеров ..> СистемныйКонтроллер : <<создает>>

    КонтейнерЗависимостей ..> БазовыйРепозитории : <<создает>>

}

@enduml